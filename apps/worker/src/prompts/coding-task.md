Build or update your homepage at `/workspace/index.html`. Start by listing assets you want (tiles, gifs, wordart matching your obsessions), then write the page, then validate, then call `done`.

You have 3 iterations. Make them count.
